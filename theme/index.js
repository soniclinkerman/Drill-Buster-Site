import { colors } from "./colors.theme";
import { breakpoints, spacing, mq, sizes } from "./layout.theme";

const defaultTheme = {
  colors,
  breakpoints,
  spacing,
  mq,
  sizes,
};

export default defaultTheme;
